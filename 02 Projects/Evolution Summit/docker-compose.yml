services:
  summit:
    build: .
    container_name: evolution-summit
    ports:
      - "3333:3000"
    volumes:
      - ./data.js:/app/data.js
      - ./imagens:/app/imagens
    restart: unless-stopped
